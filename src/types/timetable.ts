export type { Course, NewCourse, UserCourse } from "@/lib/db/schema";
export {
	DAYS,
	PERIODS,
	CATEGORIES,
	FEATURES,
	SEMESTERS,
	isDayOfWeek,
	isCategory,
	isFeature,
} from "@/types/course-domain";
export type {
	DayOfWeek,
	Category,
	Feature,
	Semester,
} from "@/types/course-domain";
